api:
  env:
    SECRET_KEY: ci-test-secret
  volumes:
  - name: gha-cached
    hostPath:
      path: /cache/ref-config
  volumeMounts:
  - name: gha-cached
    mountPath: /ref
    readOnly: true

service:
  type: NodePort

flower:
  podSecurityContext:
    runAsUser: 1000

defaults:
  podSecurityContext:
    runAsUser: 1000
  volumes:
  - name: gha-cached
    hostPath:
      path: /cache/ref-config
  - name: tmp
    emptyDir: {}
  volumeMounts:
  - name: gha-cached
    mountPath: /ref
    readOnly: false
  - name: tmp
    mountPath: /tmp
    readOnly: false

dragonfly:
  extraArgs:
  - --proactor_threads=1
  - --maxmemory=256mb
