{{- /* Migrations write the database, so use the orchastrator defaults. */ -}}
{{- $migrate := mergeOverwrite (deepCopy .Values.defaults) (omit (.Values.orchestrator | default dict) "enabled" "resources") -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ref.fullname" . }}-migrate
  labels:
    {{- include "ref.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-5"
    "helm.sh/hook-delete-policy": before-hook-creation
stringData:
  {{- tpl (toYaml .Values.defaults.env) . | nindent 2 }}
---
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "ref.fullname" . }}-migrate
  labels:
    {{- include "ref.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-4"
    "helm.sh/hook-delete-policy": before-hook-creation
spec:
  backoffLimit: 3
  template:
    metadata:
      labels:
        {{- include "ref.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.defaults.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      restartPolicy: Never
      containers:
      - name: migrate
        image: "{{ .Values.defaults.image.repository }}:{{ .Values.defaults.image.tag }}"
        imagePullPolicy: {{ .Values.defaults.image.pullPolicy }}
        command: ["ref", "db", "migrate"]
        {{- with .Values.defaults.securityContext }}
        securityContext:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        envFrom:
        - secretRef:
            name: {{ include "ref.fullname" . }}-migrate
        {{- with .Values.defaults.extraEnvFrom }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with $migrate.volumeMounts }}
        volumeMounts:
          {{- toYaml . | nindent 10 }}
        {{- end }}
      {{- with $migrate.volumes }}
      volumes:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.defaults.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.defaults.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.defaults.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
