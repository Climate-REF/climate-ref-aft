{{- range $instance, $spec := (omit .Values.providers "defaults") -}}
{{- $spec := include "ref.providerSpec" (dict "root" $ "spec" $spec) | fromYaml -}}
{{- include "ref.providerSecret" (dict "root" $ "provider" $instance "spec" $spec) }}
---
{{ end -}}
