Drop the redundant ``kubectl wait --for=condition=Ready pod`` from the Helm end-to-end ``Validate API`` step. ``kubectl rollout status`` already gates on readiness, and the extra wait matched the terminating old pod and failed the job intermittently even when the rollout succeeded.
