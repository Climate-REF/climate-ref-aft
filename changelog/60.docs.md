Adds operator runbooks for bootstrapping a deployment and for running and triaging a solve,
and a small single-node example values file they are written against.
Documents that `/ref/cache/mamba` must be writable by every worker,
because micromamba takes a lock there on each run.
