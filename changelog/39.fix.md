Render `api.httpRoute.filters` and `flower.httpRoute.filters` on the HTTPRoute rule.
Filters set in values were silently dropped, so a route intended to sit behind a forward-auth Middleware was reachable without it.
