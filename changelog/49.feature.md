Adds `api.priorityClassName` and `defaults.priorityClassName`, so the API and the workers can be given
separate scheduling priorities.
The worker value is overridable per instance, letting one provider sit below the rest.
