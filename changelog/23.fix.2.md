Fixes per-provider Helm values being silently ignored when the same key had a non-empty value under `defaults`. Provider blocks now take precedence, which is what `helm/README.md` has always documented. The four shipped values files render identically, so no existing deployment changes.
