Fixes `dragonfly.enabled=false` aborting the Helm render. The broker URL now resolves through a `ref.brokerUrl` helper, so disabling the bundled subchart and pointing `externalBroker.url` at your own Celery broker works. Flower skips its broker wait init container in that mode.
