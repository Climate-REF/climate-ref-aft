Workers now stop gracefully across rollouts, with a termination grace period covering each provider's task time limit and a Recreate strategy, and every pod satisfies the restricted Pod Security Standard.
