Add a CI-gated test that enforces consistent component version pins across ``pyproject.toml``, ``versions.toml``, the Helm chart, and ``docker-compose.yaml``, and track the frontend image version in ``versions.toml``.
