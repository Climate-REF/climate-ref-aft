Removed the untested Ingress templates in favour of HTTPRoute, removed the no-op `flower.autoscaling` block, and moved `helm/local-test-values.yaml` to `helm/ci/local-values.yaml` so packaged charts stop shipping local test config.
