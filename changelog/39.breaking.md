Move the orchestrator from `providers.orchestrator` to a top-level `orchestrator` block, so it can carry wider access to `/ref` than a diagnostic worker needs.
Provider workers now default to a read-only `/ref` with a shared read-write `/ref/scratch`, while the orchestrator and the migrate Job keep write access to the whole tree.
A leftover `providers.orchestrator` key fails the render with a message pointing at the new block.
