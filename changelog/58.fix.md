Fixed the paths of the Helm chart that rendered broken output: the provider HPA now scales on the metrics named in values instead of a Service the chart never creates, the db-migrate hook carries `imagePullSecrets`, resources and a deadline so it cannot block installs, Flower's container port no longer tracks the Service port, the ServiceMonitor selector survives chart bumps, and a `celeryRoutes` change now restarts the API and workers.
