Fixes the stale versions in the README install command and the Helm chart parameter reference, and adds tests so documented versions can no longer drift from the pinned components. The README install version is now rewritten by `bump-my-version` on release. Also replaces the fixed 60 second sleep in the Helm end-to-end CI job with a wait on the orchestrator's readiness condition, matching what `scripts/e2e-minikube.sh` already does.
